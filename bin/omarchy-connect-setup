#!/bin/bash

# omarchy:summary=Sign in to Maslow Connect and enroll this device

set -euo pipefail

if [[ ! -x /usr/bin/maslow-connect ]]; then
  echo "Maslow Connect is not installed. Update Maslow OS or install the maslow-connect package." >&2
  exit 127
fi

exec /usr/bin/maslow-connect login


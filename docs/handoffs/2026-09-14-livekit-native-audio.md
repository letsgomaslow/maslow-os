# LiveKit native audio recovery

This continues the [installed handoff checkpoint](2026-09-14-livekit-installed-handoff.md). The scope is real spoken LiveKit Expressive conversation and Hermes delegation in the existing installed Maslow AI-OS guest before Lenovo testing. No ISO rebuild, UTM, signing, publication, OTA or Lenovo change is included.

## Diagnosis and retained failures

The original provider transported local microphone audio through a published LiveKit room into another local AgentSession, then returned synthesized audio through the room to the same computer. In this x86 QEMU/TCG guest, serialized native APM processing plus the room's awaited audio submission could not consistently keep up with 20 ms capture. The full-room profile measured median capture submission of 16.393 ms and median APM work of 8.245 ms. Profiling overhead and emulated rendering contention limit the interpretation; these are not physical laptop timings.

Increasing QEMU's translation cache from 64 to 256 MiB reduced translation-cache flushes, and limiting the software compositor to two guest CPUs reduced its measured CPU use from about 694% to 194%. Quickshell used its software renderer and the actual Voice panel was inspected in QMP screenshots. These are temporary QA resource controls, not production configuration or UI performance acceptance. Neither they nor a separate 40 ms audio-submission experiment passed the original silent-intake gate with the room provider. All failures remain saved independently.

The direct-audio experiment uses the SDK's supported custom AgentSession input/output interface while retaining LiveKit cloud inference, Expressive mode, the same models/voice, strict task tool, native PortAudio, and WebRTC echo/noise processing at 48 kHz. It removes the local audio trip through two room participants. The microphone then sustained about 50 frames per second without drops.

Two further issues emerged during the experiment:

- The first output adapter trusted a virtual ALSA/PortAudio latency value of about 89,478 seconds. An independent zero-audio driver probe reproduced this impossible value. Upstream ALSA backend arithmetic can produce this unsigned-underflow pattern; the exact virtual-device negotiation that triggers it was not established. The original room provider did not use this timing metadata. The correction uses exact consumed-sample counts and a bounded device-latency estimate; malformed metadata cannot hold completion for hours. A 985-sample regression demonstrates why integer millisecond rounding is insufficient.
- With AgentSession's default three-second AEC warmup, the overlapping question was transcribed as “While” and “for packing lightly.” SDK 1.8.1 deliberately replaces STT-bound audio with silence in that first speaking interval. Explicit zero warmup preserves input while retaining Maslow's local APM. The next otherwise-identical run heard the complete question and replied while Hermes worked. It interrupted the acknowledgment before an observed speaking state, so that run alone does not establish every timing of interruption after playback has begun. Physical speaker echo remains a separate check.

One diagnostic helper originally divided a request phase that included 15 seconds of injection plus 30 seconds of waiting by only 30 seconds. Its reported 44.63 fps is invalid; elapsed-time-corrected throughput was about 29.5 fps. The original result and a caveat file are retained, and the future helper was corrected. It was not rerun and is not acceptance evidence.

## Passing experiment

The frozen v3 experiment passed the original spoken task, independent spoken conversation, and 60-second audio soak with the desktop running. It used the installed VoiceService, actual virtual microphone/speaker, real LiveKit model tool and normal Hermes coordinator. Only the provider factory was replaced with the QA direct-audio adapter; therefore this is an experiment, not installed-package acceptance.

- Task: `cd8f6735-d9e7-4b39-9c92-b7def42e935c`; Hermes run: `run_881b08c872cc43639244ba25244d9af7`.
- Exactly one task was created. Stored source matched the captured spoken generation and project. Hermes created `result.html`, 492 bytes, SHA-256 `1743fc5538c9849f2360881db967d2fcdc8c81f08c6e3fce11e257b68eb647b0`, with the requested green heading and all four checklist items.
- The complete follow-up was “While that task runs, tell me two short tips for packing lightly.” Maslow replied with two concrete tips before the task completed; non-silent audio reached the virtual speaker monitor.
- The ten-second intake check forwarded 495 frames, 49.46 fps, with zero drops and a drained queue. The entire audio run forwarded 6,886 frames with zero capture drops, zero direct-input discards and zero provider errors. Normal End Voice and service shutdown completed.
- Coordinator startup was deliberately completed before this experiment's audio began. Cold first-task startup during speech is a separate gate.

## Local evidence and provenance

The correction is implemented in runtime commit `295a489dcd86c1e4ba68f781eab3eb3118d00a86`, with package recipe commit `be9be362edfc52527ed995f62650d1bd00c79ddb`. The built, unpublished package is `maslow-voice-0.1.4-12-x86_64.pkg.tar.zst`, SHA-256 `2288b2294475b507556c0b900dfbc018027a98b3c59b5a63bce06f2fe740c278`. It installed normally in the reused guest with dependency checking. Package build inputs, hashes, check output and archive are in `../linux-live-acceptance/build/revision12/`. The existing build helper uses `makepkg --nodeps`; this is a build-environment limitation, not a substitute for the normal guest installation check. No dependency, provider setting, task schema or deployment channel changed.

The production factory now selects the native provider. The room provider and private Mac tester remain unchanged. Native I/O loads the optional SDK lazily, ends queued microphone input on mute/failure, notifies AgentSession when input is disabled, and closes all owned session/inference/HTTP resources even if output cleanup fails. Transport-first daemon shutdown remains unchanged. A blocked capture cannot revive a segment after output closes.

Python 3.14.4 with the pinned SDK passed the complete Voice suite: 223 tests, 221 passed and two platform skips. The installed x86_64 package passed all 54 focused native audio, transport, LiveKit startup/schema and provider tests on Python 3.14.7 in 65.729 seconds. Package syntax and `test/maslow-packaging` passed. An independent Sol review found no material issue in the native I/O lifecycle or SDK contract; Terra implemented the bounded provider/tests and Luna reviewed the upstream behavior. These checks do not themselves establish live spoken acceptance.

### Test infrastructure and helper failures

The first package build stalled during the import check while the installed VM was also running. Docker control requests became slow. The owned build was stopped (exit 137); the incomplete output is preserved in `revision12-interrupted/`. During the subsequent container pause/unpause recovery, the guest unexpectedly rebooted. The previous-boot kernel excerpt did not identify a cause. This happened after the passing v3 experiment and before package 12 was installed; it is not evidence of a package-12 application failure. The pause/reboot sequence also means the earlier guest RAM-only credentials were lost. The initial credential cleanup attempt timed out at SSH and did not complete its scan; do not present that file as a successful cleanup. The ordinary harness then stopped the guest. Building separately with the owned build container limited to 1,536 MiB succeeded with the same source, dependencies and checks. Unrelated containers were not stopped or restarted. The build container was stopped before the new guest acceptance work.

The first package-12 scenario helper lacked a `subprocess` import in its new provenance check. It failed before creating the project, starting Voice or submitting work. The failed helper/log/result were retained, then the import was corrected. The next full run reached the actual installed Voice panel and microphone-on state, but its warmup forwarded 741 frames and dropped 33; the unchanged silent-settle gate failed. QMP screenshots were being collected during the timing window. Whether screenshot work caused the loss is unconfirmed. Code comparison found no added production work in the silent microphone path; the losses were measured at the upstream PortAudio queue. A distinct retest excludes external display capture during all audio checks and preserves every original acceptance threshold.

The first digital-echo helper used a 30-second control startup deadline, shorter than the established installed harness's 180 seconds. It timed out before the prompt/audio test began and shut down cleanly. The corrected helper retains all echo/audio checks and uses the existing 180-second startup allowance with a 240-second overall bound. No failed echo or audio quality result is converted into a pass by this correction.

### Actual playback interruption failure

The package-12 retest without display capture passed the unchanged ten-second intake gate: 500 frames in 10.089 seconds, 49.56 fps, zero drops and an empty queue. It created exactly one real task, `df762aa5-e7ff-4a60-b5b4-f2883973583e`, with Hermes run `run_49d869786e6e49c69fb9a5bdf273b706`. Independent inspection confirmed `result.html`, 427 bytes, SHA-256 `32dcb0036315a6f550ac36f5de54e0e3706c9bfd79ca87e916e641b65184f53e`, with the requested green heading and four checklist items.

The acknowledgment began playing while the follow-up question was being injected. This time the question was split into two final transcripts, and no final packing reply arrived before the unchanged timeout. The task completed, but conversation acceptance failed with `FOLLOW_UP_CONCURRENT_RESPONSE_NOT_OBSERVED`. The run forwarded 4,825 frames and dropped nine, with zero provider errors. Normal End Voice and service cleanup completed. The passing intake does not establish that display capture was the sole cause of the preceding warmup failure.

An independent deterministic SDK regression then reproduced a native output cancellation bug: cancelling a new capture task could propagate into the shared previous-segment finisher or transport clear. The SDK retained a pending playback count and `wait_for_playout()` hung. Runtime commit `b816c10d` shields these two shared waits while allowing the cancelled producer to exit immediately. Regression tests verify exactly one interrupted completion, a zero pending count, subsequent segment playback, and completion of the physical clear. Root verification passed 225 tests, with two platform skips. This is a confirmed lifecycle defect consistent with the installed symptom; a new installed retest is required to establish that it resolves the live failure.

Before the next build, the guest credential cleanup scanned 8,368 owned QA/Voice regular files up to 32 MiB, skipped 32 larger files, found zero exact credential matches, deleted all three keyring values and removed the RAM-only Hermes environment. The guest then stopped through the normal harness. This successful bounded scan is separate from the earlier cleanup that timed out before the unexpected reboot.

Package 13 contains the cancellation correction: runtime `b816c10de951793e93310f80f234cc917acd0320`, recipe `769e1f3e30e63642d74a6035a5361051c6b161f7`, package SHA-256 `70d5b3da6ee72335f5d75c512bdac3ae9936c6c9f8c314a00a02d32c360102e9`. It installed normally and passed all 56 focused installed checks in 70.143 seconds. Initial QA setup reached the compositor before all threads/session components had started, so two attempts failed renderer-affinity and software-backend preconditions before Voice or a project started. The helper's final precondition check also raised, leaving only the preserved failure logs. Reapplying the existing resource controls after desktop startup made the unchanged preconditions pass; no production setting changed.

The actual package-13 run still failed the concurrent reply gate. It heard the complete packing question and submitted task `15d1c5e6-bcd5-4e5f-aadc-5b9906bb8e05`, which reached completed state. Passive output diagnostics establish that the interrupted acknowledgment finished at 160.246 seconds with 0.956083 seconds consumed. At failure, the SDK pending playback count was zero and the finisher was done, not cancelled, with no error. Thus the reproduced cancellation defect is fixed, but it was not the sole cause of the live failure. The microphone forwarded 4,668 frames and dropped 174 overall, having dropped zero through the task request. There were zero provider errors. The full question arrived at 162.8 seconds, followed by thinking then listening, with no final reply. Independent artifact verification for this failed run is recorded below; it does not repair the failed conversation gate.

### Truthful microphone startup state

The second echo helper failed before sending its prompt because public microphone state was false despite 64 captured input packets and an active native provider. A deterministic reproduction established a product lifecycle race: AgentSession's startup listening event captured microphone false and queued delivery; native startup then directly emitted microphone true; the older queued event could overwrite that correct public state indefinitely. Waiting longer does not repair it without a later state change.

Runtime commit `face62c3` adds a native-only readiness guard. SDK state callbacks cannot publish until AgentSession and the requested transport are ready; readiness resets on start, failure and stop. Typed audio-disabled sessions still publish later SDK states once ready. Regression tests drain queued events after startup and verify microphone-on remains true, plus typed startup and stop/restart behavior. Root verification passed 227 tests with two platform skips in 6.192 seconds. The room provider and private Mac tester remain unchanged. The inherited typed-turn state helper already preserves the actual microphone value, so it needed no override.

The next package-14 run includes this correction and additional passive SDK diagnostics. Normal spoken acceptance, cold coordinator startup during speech, and digital echo acceptance remain open at this checkpoint. No failed gate has been relaxed.

Package 14 is built from runtime `face62c3dcb06e57f4e57f475eb8afc09beb227c` and recipe `e6d9cc2696e17df622bea41ccb9633663961bf54`, SHA-256 `00196e9654ead176c997d2f3503e572d3c2290918488118982909e448cf0228e`. The normal upgrade passed, followed by all 17 installed native-provider tests in 41.314 seconds. Source, builder and VM were validated sequentially; the owned build container was stopped before guest startup. Package-13 credential cleanup had scanned 8,745 scoped files, skipped 33 larger files, found zero matches and removed all three credentials and the RAM-only Hermes environment before shutdown.

Independent post-run inspection subsequently confirmed package-13 task `15d1c5e6-bcd5-4e5f-aadc-5b9906bb8e05` used run `run_847aef28067f4db399cb67c8a17ca3df` and produced the exact `result.html`, 392 bytes, SHA-256 `9cf1676a4bb5cfe71b63d3b29f1f580d448a0f76d974c86eb6ea067cf2b39ae2`, with the green heading and all four items. It does not change that run's failed spoken conversation result.

The first package-14 diagnostic attempt found Hyprland running but Quickshell absent, and failed before Voice or a project started. No coredump, kernel OOM/segfault entry or supervisor relaunch message was found in the bounded checks; the exit cause is unresolved. Restarting through the existing software-rendering QA wrapper restored Quickshell. The preserved `native-package14-desktop-precondition` log/result remains a failed precondition, not a live-provider test. The distinct retry uses the same full gates and bounded SDK observers, with no QMP capture during audio.

### Package-14 diagnostic: false speaking events and premature handoff

The diagnostic helper ran with SHA-256 `4cb6157d85e66baa240d888fc30aecf88b6d354de0393cdd235bbfa9a49f3692`. Native startup published a single listening event with microphone true. The unchanged intake gate forwarded 500 frames at 49.14 fps with zero drops. The SDK created eight speech handles and marked all eight interrupted. Only the first generation produced LLM metrics (88 output tokens, the real task function call); its function output required a reply, but no TTS generation, playback segment or assistant conversation item followed. At failure all speech tasks were done, no speech was queued, authorization was allowed, native I/O was open, and there were zero SDK/observer errors. This is repeated cancellation, not a stranded playback finisher or a proven empty follow-up model response.

Public user-state observers showed repeated brief speaking/listening transitions during the nominally silent warmup, before any WAV injection, and again after the fixture. Those transitions coincided with cancellation of later reply handles. The full run forwarded 5,222 frames with 16 drops before failure. The normal End Voice and service cleanup completed with the desktop still active. A separate 25-second installed PortAudio/APM silence probe then observed 1,249 raw frames and 1,249 processed frames, every sample exactly zero, with no cloud session or speaker output. This isolates the device/APM silence behavior; the exact source of the false cloud-session speaking events still requires correlation.

The same run also exposed premature task dispatch. At 165.5 seconds, before the request fixture finished at 172.8, the provider submitted task `00e6010e-a511-4c2a-b901-cc534b202bb8` using only the first sentence asking for `result.html`. Hermes run `run_5aae1aee411f4404a6fab731c53b9b0c` completed, but independent inspection found only a generic Result page, 248 bytes, SHA-256 `c8c85dc639bea1212320ed590dfaaef879d3b1215acad2bae798c5b7bbc8fed9`. It lacked the later spoken heading, green styling and checklist instructions. Both conversation continuation and task-detail accuracy therefore failed. Preserve this result rather than treating a completed Hermes task as acceptance.

Evidence root: `/Users/r.david/.codex/visualizations/2026/09/13/01a09a02-5898-7873-8684-032cf60f9165/livekit-audio-recovery-0914/`. Machine-local diagnostics and synthetic artifacts are not committed or published.

- `native-experiment-comparison.json` and separate `native-cache256`, `native-software`, `native-renderer-limited`, `native-batch40` logs/results preserve failed unchanged room-provider checks.
- `cache-comparison.json`, `desktop-cpu-before.json`, `desktop-cpu-limited.json`, `real-latency-renderer-limited.json` and `real-latency-measurement-caveats.json` separate scheduling diagnostics from acceptance.
- Frozen direct adapter v1/v2/v3 files and their results retain the device-latency hang, overlap loss and passing experiment. V3 adapter SHA-256: `d867398f2f5d5494e24e3285d046043fc753da60d9bb6e48b6489cb100f48cb5`; scenario SHA-256: `812edac7a1bf1b14d77b44174be9e422432953d2e05a4c685c028aa9228e83e3`.
- `native-direct-v3-result.json`, matching log and `direct-v3-artifact-review.json` contain the passing trace and independent synthetic artifact inspection. Contract and fractional-sample regression logs retain exact checks.
- QMP images are under the sibling `linux-live-acceptance/installed/guest/` directory. The software-rendered Voice panel is visually present; all orb placement/state transitions and desktop performance have not been accepted.

The guest is the reused September 8 installation described in the preceding handoff, with eight virtual CPUs and 2 GiB RAM. Harness checkout remains `d96a3ee563df5a1eb76a711c7b408c0d1d0137cb`; original ISO SHA-256 remains `c3ee08889eaa50bf2843ae6d0bdc45f8549569f956159421b1e67b98a31b363a`. QEMU 11.1.1 runs directly in the ARM64 tools container, with a QA-only 256 MiB translation cache. This is neither a fresh install nor Lenovo hardware proof.

## Sources

The pinned [AgentSession implementation](https://github.com/livekit/agents/blob/livekit-agents%401.8.1/livekit-agents/livekit/agents/voice/agent_session.py) and [audio I/O contract](https://github.com/livekit/agents/blob/livekit-agents%401.8.1/livekit-agents/livekit/agents/voice/io.py) support custom input/output and require accurate segment completion. The [agent activity implementation](https://github.com/livekit/agents/blob/livekit-agents%401.8.1/livekit-agents/livekit/agents/voice/agent_activity.py) implements AEC warmup suppression. The [PortAudio ALSA backend](https://github.com/PortAudio/portaudio/blob/v19.7.0/src/hostapi/alsa/pa_linux_alsa.c) contains the latency subtraction relevant to the observed invalid driver value. These sources explain mechanisms; the local runs establish the behavior observed here.
